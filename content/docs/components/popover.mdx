---
title: Popover
description: Conteúdo flutuante posicionado sobre Base UI.
---

import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover';
import { Button } from '@/components/ui/button';

<Preview>
  <Popover>
    <PopoverTrigger render={<Button variant="outline">Popover</Button>} />
    <PopoverContent className="w-64">
      <p className="text-sm text-muted-foreground">
        Conteúdo flutuante posicionado com Base UI.
      </p>
    </PopoverContent>
  </Popover>
</Preview>

## Instalação

```bash
npx shadcn@latest add https://<host>/r/popover.json
```

## Uso

```tsx
import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover';
import { Button } from '@/components/ui/button';

export function Example() {
  return (
    <Popover>
      <PopoverTrigger render={<Button variant="outline">Abrir</Button>} />
      <PopoverContent>Conteúdo</PopoverContent>
    </Popover>
  );
}
```
