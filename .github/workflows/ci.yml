name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # O browser project do vitest roda num Chromium de verdade — sem este
      # passo ele não sobe no runner.
      - name: Install Chromium (Playwright)
        run: bunx playwright install --with-deps chromium

      - name: Format
        run: bun run format:check

      - name: Lint
        run: bun run lint

      # O typecheck do apps/docs resolve @adila-sh/ui pelos tipos em
      # packages/ui/dist, então o build precisa vir antes.
      - name: Build
        run: bun run build

      - name: Typecheck
        run: bun run typecheck

      - name: Test with coverage thresholds
        run: bun run test:coverage

      - name: Test status generator
        run: bun run --cwd apps/docs test:test-status
