---
title: Combobox
description: Componente combobox do design system adila.co, sobre Base UI.
---

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from '@adila-sh/ui';

export const frameworks = ['Next.js', 'Remix', 'Astro', 'SvelteKit', 'Nuxt'];

<Preview>
  <div className="w-64">
    <Combobox items={frameworks}>
      <ComboboxInput placeholder="Buscar framework..." />
      <ComboboxContent>
        <ComboboxEmpty>Nenhum resultado.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  </div>
</Preview>

## Instalação

```bash
npx shadcn@latest add https://ds.adila.co/r/combobox.json
```

## Uso

```tsx
import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from '@adila-sh/ui';

const frameworks = ['Next.js', 'Remix', 'Astro', 'SvelteKit', 'Nuxt'];

export function Example() {
  return (
    <Combobox items={frameworks}>
      <ComboboxInput placeholder="Buscar framework..." />
      <ComboboxContent>
        <ComboboxEmpty>Nenhum resultado.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  );
}
```

## Dependências

- `@base-ui/react`
- `lucide-react`

## Componentes relacionados

- [Button](/docs/components/button)
- [Input Group](/docs/components/input-group)
