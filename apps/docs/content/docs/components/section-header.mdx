---
title: Section Header
description: Componente section-header do design system adila.co.
---

import {
  SectionHeader,
  SectionHeaderActions,
  SectionHeaderContent,
  SectionHeaderDescription,
  SectionHeaderTitle,
} from "@adila-sh/ui";
import { Button } from "@adila-sh/ui";

<Preview>
  <SectionHeader className="w-full">
    <SectionHeaderContent>
      <SectionHeaderTitle>Últimas transações</SectionHeaderTitle>
      <SectionHeaderDescription>
        Movimentações registradas nos últimos 30 dias.
      </SectionHeaderDescription>
    </SectionHeaderContent>
    <SectionHeaderActions>
      <Button variant="outline" size="sm">
        Ver todas
      </Button>
    </SectionHeaderActions>
  </SectionHeader>
</Preview>

## Instalação

```bash
bun add @adila-sh/ui
```

## Uso

```tsx
<SectionHeader>
  <SectionHeaderContent>
    <SectionHeaderTitle>Últimas transações</SectionHeaderTitle>
    <SectionHeaderDescription>
      Movimentações registradas nos últimos 30 dias.
    </SectionHeaderDescription>
  </SectionHeaderContent>
  <SectionHeaderActions>
    <Button variant="outline">Ver todas</Button>
  </SectionHeaderActions>
</SectionHeader>
```
