---
title: File Upload
description: Componente file-upload do design system adila.co, sobre Base UI.
---

import { FileUpload } from "@/components/ui/file-upload";

<Preview>
  <FileUpload
    className="w-full max-w-xl"
    accept="image/png,image/jpeg,application/pdf"
    maxSize={5 * 1024 * 1024}
    multiple
  />
</Preview>

## Instalação

```bash
npx shadcn@latest add https://ds.adila.co/r/file-upload.json
```

## Uso

```tsx
import { FileUpload } from "@/components/ui/file-upload";

<FileUpload
  accept="image/png,image/jpeg,application/pdf"
  maxSize={5 * 1024 * 1024}
  multiple
  onFilesChange={(files) => console.log(files)}
/>;
```

Use `value` e `onFilesChange` para controlar a lista externamente. Arquivos
maiores que `maxSize` são rejeitados com uma mensagem de erro.

## Dependências

- `@phosphor-icons/react`

## Componentes relacionados

- [Button](/docs/components/button)
