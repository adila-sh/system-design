---
title: Button Group
description: Componente button-group do design system adila.co.
---

import { Button } from '@adila-sh/ui';
import {
  ButtonGroup,
  ButtonGroupSeparator,
} from '@adila-sh/ui';
import { Bold, Italic, Underline } from 'lucide-react';

<Preview>
  <ButtonGroup>
    <Button variant="outline" size="icon" aria-label="Negrito">
      <Bold />
    </Button>
    <Button variant="outline" size="icon" aria-label="Itálico">
      <Italic />
    </Button>
    <ButtonGroupSeparator />
    <Button variant="outline" size="icon" aria-label="Sublinhado">
      <Underline />
    </Button>
  </ButtonGroup>
</Preview>

## Instalação

```bash
bun add @adila-sh/ui
```

## Uso

```tsx
import { Button } from '@adila-sh/ui';
import {
  ButtonGroup,
  ButtonGroupSeparator,
} from '@adila-sh/ui';
import { Bold, Italic, Underline } from 'lucide-react';

export function Example() {
  return (
    <ButtonGroup>
      <Button variant="outline" size="icon" aria-label="Negrito">
        <Bold />
      </Button>
      <Button variant="outline" size="icon" aria-label="Itálico">
        <Italic />
      </Button>
      <ButtonGroupSeparator />
      <Button variant="outline" size="icon" aria-label="Sublinhado">
        <Underline />
      </Button>
    </ButtonGroup>
  );
}
```

## Dependências
- `class-variance-authority`

## Componentes relacionados

- [Separator](/docs/components/separator)
