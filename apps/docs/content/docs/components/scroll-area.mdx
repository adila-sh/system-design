---
title: Scroll Area
description: Componente scroll-area do design system adila.co, sobre Base UI.
---

import { ScrollArea } from '@adila-sh/ui';

<Preview>
  <ScrollArea className="h-40 w-full max-w-sm rounded-lg border">
    <div className="flex flex-col gap-2 p-4">
      <p className="text-sm font-medium">Versões</p>
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} className="text-sm text-muted-foreground">
          Versão 1.{20 - i}.0
        </div>
      ))}
    </div>
  </ScrollArea>
</Preview>

## Instalação

```bash
npx shadcn@latest add https://ds.adila.co/r/scroll-area.json
```

## Uso

```tsx
import { ScrollArea } from '@adila-sh/ui';

export function Example() {
  return (
    <ScrollArea className="h-40 w-full max-w-sm rounded-lg border">
      <div className="flex flex-col gap-2 p-4">
        <p className="text-sm font-medium">Versões</p>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} className="text-sm text-muted-foreground">
            Versão 1.{20 - i}.0
          </div>
        ))}
      </div>
    </ScrollArea>
  );
}
```

## Dependências

- `@base-ui/react`
