---
title: Bubble
description: Componente bubble do design system adila.co, sobre Base UI.
---

import { Bubble, BubbleContent, BubbleGroup } from '@adila-sh/ui';

<Preview>
  <BubbleGroup className="w-full max-w-xs">
    <Bubble variant="muted" align="start">
      <BubbleContent>Oi! Tudo bem por aí?</BubbleContent>
    </Bubble>
    <Bubble align="end">
      <BubbleContent>Tudo ótimo, e com você?</BubbleContent>
    </Bubble>
    <Bubble variant="muted" align="start">
      <BubbleContent>Também! Bora marcar aquela call?</BubbleContent>
    </Bubble>
  </BubbleGroup>
</Preview>

## Instalação

```bash
npx shadcn@latest add https://ds.adila.co/r/bubble.json
```

## Uso

```tsx
import { Bubble, BubbleContent, BubbleGroup } from '@adila-sh/ui';

export function Example() {
  return (
    <BubbleGroup className="w-full max-w-xs">
      <Bubble variant="muted" align="start">
        <BubbleContent>Oi! Tudo bem por aí?</BubbleContent>
      </Bubble>
      <Bubble align="end">
        <BubbleContent>Tudo ótimo, e com você?</BubbleContent>
      </Bubble>
      <Bubble variant="muted" align="start">
        <BubbleContent>Também! Bora marcar aquela call?</BubbleContent>
      </Bubble>
    </BubbleGroup>
  );
}
```

## Dependências

- `@base-ui/react`
- `class-variance-authority`
