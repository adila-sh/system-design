---
title: Empty
description: Componente empty do design system adila.co, sobre Base UI.
---

import {
  Empty,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
  EmptyDescription,
  EmptyContent,
} from '@adila-sh/ui';
import { Button } from '@adila-sh/ui';
import { Inbox } from 'lucide-react';

<Preview>
  <Empty className="w-full max-w-sm border">
    <EmptyHeader>
      <EmptyMedia variant="icon">
        <Inbox />
      </EmptyMedia>
      <EmptyTitle>Nenhuma mensagem</EmptyTitle>
      <EmptyDescription>
        Você está em dia. Novas mensagens aparecerão aqui.
      </EmptyDescription>
    </EmptyHeader>
    <EmptyContent>
      <Button size="sm">Atualizar</Button>
    </EmptyContent>
  </Empty>
</Preview>

## Instalação

```bash
npx shadcn@latest add https://ds.adila.co/r/empty.json
```

## Uso

```tsx
import {
  Empty,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
  EmptyDescription,
  EmptyContent,
} from '@adila-sh/ui';
import { Button } from '@adila-sh/ui';
import { Inbox } from 'lucide-react';

export function Example() {
  return (
    <Empty className="border">
      <EmptyHeader>
        <EmptyMedia variant="icon">
          <Inbox />
        </EmptyMedia>
        <EmptyTitle>Nenhuma mensagem</EmptyTitle>
        <EmptyDescription>
          Novas mensagens aparecerão aqui.
        </EmptyDescription>
      </EmptyHeader>
      <EmptyContent>
        <Button size="sm">Atualizar</Button>
      </EmptyContent>
    </Empty>
  );
}
```

## Dependências

- `class-variance-authority`
