---
title: Empty
description: Componente empty do design system adila.co.
---

import {
  Empty,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
  EmptyDescription,
  EmptyContent,
} from '@adila-sh/ui';
import { Button } from '@adila-sh/ui';
import { Inbox } from 'lucide-react';

<Preview>
  <Empty className="w-full max-w-sm border">
    <EmptyHeader>
      <EmptyMedia variant="icon">
        <Inbox />
      </EmptyMedia>
      <EmptyTitle>Nenhuma mensagem</EmptyTitle>
      <EmptyDescription>
        Você está em dia. Novas mensagens aparecerão aqui.
      </EmptyDescription>
    </EmptyHeader>
    <EmptyContent>
      <Button size="sm">Atualizar</Button>
    </EmptyContent>
  </Empty>
</Preview>

## Instalação

```bash
bun add @adila-sh/ui
```

## Uso

```tsx
import {
  Empty,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
  EmptyDescription,
  EmptyContent,
} from '@adila-sh/ui';
import { Button } from '@adila-sh/ui';
import { Inbox } from 'lucide-react';

export function Example() {
  return (
    <Empty className="border">
      <EmptyHeader>
        <EmptyMedia variant="icon">
          <Inbox />
        </EmptyMedia>
        <EmptyTitle>Nenhuma mensagem</EmptyTitle>
        <EmptyDescription>
          Novas mensagens aparecerão aqui.
        </EmptyDescription>
      </EmptyHeader>
      <EmptyContent>
        <Button size="sm">Atualizar</Button>
      </EmptyContent>
    </Empty>
  );
}
```

## Dependências

- `class-variance-authority`
