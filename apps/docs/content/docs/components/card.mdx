---
title: Card
description: Contêiner de conteúdo com cabeçalho, corpo e rodapé.
---

import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@adila-sh/ui';
import { Button } from '@adila-sh/ui';

<Preview>
  <Card className="w-72">
    <CardHeader>
      <CardTitle>Lead Twin</CardTitle>
      <CardDescription>Leitura comportamental ativa</CardDescription>
    </CardHeader>
    <CardContent className="text-sm text-muted-foreground">
      Engajamento alto nos últimos 5 minutos.
    </CardContent>
    <CardFooter>
      <Button size="sm">Ver detalhes</Button>
    </CardFooter>
  </Card>
</Preview>

## Instalação

```bash
bun add @adila-sh/ui
```

## Uso

```tsx
import { Card, CardHeader, CardTitle, CardContent } from '@adila-sh/ui';

export function Example() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Título</CardTitle>
      </CardHeader>
      <CardContent>Conteúdo</CardContent>
    </Card>
  );
}
```
